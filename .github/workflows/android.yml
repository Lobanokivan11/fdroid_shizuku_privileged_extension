name: Android CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest
    permissions: write-all
    steps:
    
    - uses: actions/checkout@v4
    - name: set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle
        

    - name: CHMOD GRADLEW
      run: chmod 777 ./gradlew
    - name: Cache Gradle
      uses: burrunan/gradle-cache-action@v2
    - name: Build Release APK
      run: ./gradlew assembleRelease
    - name: Nightly build
      uses: softprops/action-gh-release@v1
      with:
        body: Debug Builds
        tag_name: github_nightly_debug
        name: Debug Builds
        files: |
           app/build/outputs/apk/release/F-DroidShizukuPrivilegedExtension-release.apk
